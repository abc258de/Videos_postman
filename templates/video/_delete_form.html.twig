{# <form method="post" action="{{ path('app_video_delete', {'id': video.id}) }}" onsubmit="return confirm('Are you sure you want to delete this video?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ video.id) }}">
    <button class="btn btn-danger">Delete</button>
</form> #}

    {# changed after removing the <a> from show.html.twig #}

    <form method="post" action="{{ path('app_video_delete', {'id': id}) }}" onsubmit="return confirm('Are you sure you want to delete this video?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ id) }}">
        <button type="submit" class="btn btn-danger btn-sm">Delete</button>
    </form>

